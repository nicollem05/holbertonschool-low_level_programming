#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - returns me the last number and grader o less
 *Return: returns me a printf with a number
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if(n%10 > 5)
		printf("is the string and is greater than 5\n", n%10);
	else if( n%10 != 0)&&( n%10 >= 6)
		printf("and is less than 6 and not 0", n%10);
	else
		(printf("and is 0", n%10);
		 return (0);
		 }
